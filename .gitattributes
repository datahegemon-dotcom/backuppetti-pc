# Shell scripts and the systemd unit must stay LF-only so they run on Linux/Mac
# even when edited from Windows (a CRLF shebang would break `curl | bash`).
*.sh        text eol=lf
*.service   text eol=lf
*.plist      text eol=lf
